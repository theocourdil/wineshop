- if current_user
  = link_to 'Déconnexion', destroy_user_session_path,
                            data: { turbo_method: :delete, turbo_confirm: 'Voulez-vous vous déconnecter ?' },
                            class: 'text-black'                        
- else
  .d-flex.px-4
    = link_to "S'enregistrer", new_user_registration_path,
                              class: "text-black" 

    = link_to "Se connecter", new_user_session_path, 
                              class: "text-black" 